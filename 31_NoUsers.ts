const usernames2: string[] = ['olivia', 'emma', 'admin', 'amelia', 'ava', 'sophia', 'isabella'];

if (usernames2.length == 0) {
    console.log("We need to find some users!");
} else {
    usernames2.splice(0);
}